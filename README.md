# ManmeetCodingChallenge
This is the repo of the coding challenge given by Company sipal penco

la parte del frontend del web-app si trova nella cartella chiamata "fontend"
invece la parte del backend si trova nella cartella chiamta "backend"

#Procedura per avviare il clinet(fron-end)
1---> npm install
2--->npm run dev

#Procedura per avviare il server(back-end)
1-->npm install
2-->nodemon index.js
